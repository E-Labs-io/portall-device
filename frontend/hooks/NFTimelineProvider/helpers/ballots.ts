/** @format */

import { Ballot } from "../types";

export const getBallots = async (): Promise<Ballot[]> => {
  let ballots: Ballot[];
  return ballots;
};

export const getBallotData = async (ballotTitle: string): Promise<Ballot> => {
  let ballot: Ballot;
  return ballot;
};
